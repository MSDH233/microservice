spring:
  cloud:
    config:
      label: master
      uri: http://127.0.0.1:5555
      name: provider
      profile: 6222
      discovery: true

management:
  security:
    enabled: false
eureka:
  client:
    service-url:
      defaultZone: http://eureka7778.com:7778/eureka/,http://eureka7779.com:7779/eureka/,http://eureka7779.com:7777/eureka/
  instance:
    instance-id: ${spring.application.name}:${server.port}

